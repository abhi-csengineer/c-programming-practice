#include <stdio.h>
int main (void)
{
int a,b;
printf("Enter the first no a:\n");
printf("Enter the second no b:\n");
scanf("%d %d",&a,&b);
printf("=The answer is %d" a+b); //Here we can sub,multiple,div by replacing the addition sign with them.
return 0;
}
